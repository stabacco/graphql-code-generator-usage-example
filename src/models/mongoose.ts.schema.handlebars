/* tslint:disable */

import { Model } from 'mongoose';
{{#each types }}
{{#ifDirective this "entity" }}
{{#unless embedded }}
import { {{ ../name }}DbObject } from './{{toLowerCase ../name }}.type';
{{/unless}}
{{/ifDirective}}
{{/each}}

{{#each types }}
{{#ifDirective this "entity" }}
{{#unless embedded }}
export { {{ ../name }}Model } from './{{toLowerCase ../name }}.type';
{{/unless}}
{{/ifDirective}}
{{/each}}